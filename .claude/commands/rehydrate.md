---
description: Rehydrate from the session handoff and resume the paused effort
---
Rehydrate context from the session handoff and resume the paused work.

1. Read @docs/design/SESSION-HANDOFF.md in full.
2. Follow the instructions in its "Revival prompt (paste after /clear)" section.
3. Confirm the current state from `git log` first (the handoff lists expected commit hashes — verify they match before trusting the rest).
4. Briefly summarize where things stand, then await my instruction.

If I passed arguments, treat them as which piece to tackle next: $ARGUMENTS
